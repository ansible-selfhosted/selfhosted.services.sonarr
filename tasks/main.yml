---
# skip jinja testing
- name: Get user info for "{{ ansible_user_id }}"
  ansible.builtin.user:
    user: "{{ ansible_user_id }}"
    state: present
  register: user_info

- name: Normalize default paths
  ansible.builtin.set_fact:
    sonarr_config_path: "{{ sonarr_config_path | replace('~', user_info.home) }}"
    sonarr_data_path: "{{ sonarr_data_path | replace('~', user_info.home) }}"

- name: Create directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0750"
    recurse: true
  loop:
    - "{{ sonarr_config_path }}"
    - "{{ sonarr_data_path }}"

- name: Put container related files on the host
  ansible.builtin.template:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    mode: "0640"
  loop:
    - { src: sonarr.container.j2, dest: "{{ user_info.home }}/.config/containers/systemd/sonarr.container" }

- name: Start the container service
  ansible.builtin.systemd:
    name: sonarr
    state: started
    scope: user
    enabled: true
    daemon_reload: true
